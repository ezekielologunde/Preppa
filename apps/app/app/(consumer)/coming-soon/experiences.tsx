import { ComingSoon } from '@/components/ComingSoon';

export default function ExperiencesPreview() {
  return (
    <ComingSoon
      title="Prep experiences"
      blurb="Cooking classes, supper clubs, and private-chef nights with local cooks. On the roadmap — not bookable yet."
    />
  );
}
