packages:
  - "apps/*"
  - "packages/*"

# NOTE: the marketing landing page (landing/) and the design prototype (the loose
# *.jsx / *.css / *.html at the repo root, plus _ds/, screenshots/, uploads/) are
# deliberately NOT workspace packages. The landing ships on its own Vercel deploy;
# the prototype is kept as a design reference only and is never built or imported.
